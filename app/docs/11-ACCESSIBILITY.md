# Accessibility

Requirements:

- semantic HTML
- keyboard navigation
- visible focus states
- accessible buttons
- accessible forms
- descriptive alt text
- proper heading hierarchy
- sufficient contrast
- reduced motion support

WebGL must never contain the only representation of important content.

Video must not prevent navigation.

Animation must never make text unreadable.

Mobile navigation must be keyboard accessible where applicable.